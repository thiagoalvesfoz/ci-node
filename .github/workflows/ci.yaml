name: api-node-application

on:
  pull_request:
    branches:
      - develop

jobs:
  run-test:
    runs-ons: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14.x
      - run: npm test
